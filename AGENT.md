# Agent Behavior & Global Rules

**CRITICAL INSTRUCTION:**
Before answering any query, generating code, or modifying files, you MUST strictly adhere to the guidelines defined in the file `./cursor/rules` located in the root directory.

1.  **Read:** Always contextually read `.cursor/rules` first.
2.  **Apply:** Ensure code style, naming conventions, and architectural patterns match the content of `./cursor/rules`.
3.  **Validate:** If a user request conflicts with `./cursor/rules`, warn the user before proceeding.

Reference file: ./cursor/rules
